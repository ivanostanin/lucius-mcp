name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  validate-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.tag_version }}
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0  # Fetch all history for all branches and tags

      - name: Verify tag is on main branch
        run: |
          # Check if the tagged commit is reachable from main
          if ! git merge-base --is-ancestor ${{ github.sha }} origin/main; then
            echo "ERROR: Tag must point to a commit on the main branch"
            exit 1
          fi

      - name: Extract version from tag
        id: get_version
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          echo "tag_version=$TAG_VERSION" >> $GITHUB_OUTPUT
      
      - name: Validate version matches pyproject.toml
        run: |
          PROJECT_VERSION=$(grep '^version = ' pyproject.toml | sed 's/version = "\(.*\)"/\1/')
          TAG_VERSION=${{ steps.get_version.outputs.tag_version }}
          
          if [ "$PROJECT_VERSION" != "$TAG_VERSION" ]; then
            echo "ERROR: Version mismatch!"
            echo "Tag version: $TAG_VERSION"
            echo "pyproject.toml version: $PROJECT_VERSION"
            exit 1
          fi

  python-quality:
    needs: validate-version
    permissions:
      contents: read
    uses: ./.github/workflows/_python_check.yml
    with:
      python-version: '3.14'

  mcpb-build:
    needs: [validate-version, python-quality]
    permissions:
      contents: read
    uses: ./.github/workflows/_mcpb_build.yml
    with:
      version: ${{ needs.validate-version.outputs.version }}
      artifact-name: mcpb-bundle-${{ needs.validate-version.outputs.version }}

  docker-build:
    needs: [python-quality, validate-version]
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_docker_build_push.yml
    with:
      image-name: lucius-mcp
      tags: |
        ${{ needs.validate-version.outputs.version }}
        latest
      labels: |
        org.opencontainers.image.source=https://github.com/${{ github.repository }}
        org.opencontainers.image.description=Release ${{ needs.validate-version.outputs.version }}
      push: true

  github-release:
    needs: [validate-version, mcpb-build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          name: mcpb-bundle-${{ needs.validate-version.outputs.version }}
          path: dist/

      - name: Get Changelog Entry
        id: changelog_reader
        uses: mindsers/changelog-reader-action@v2.2.3
        with:
          version: ${{ github.ref_name }}
          path: ./CHANGELOG.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.5.0
        with:
          body: ${{ steps.changelog_reader.outputs.changes }}
          files: dist/*.mcpb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  pages-build:
    needs: python-quality
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Prepare GitHub Pages content
        run: |
          mkdir -p _site/lucius-testops-mcp-server
          cp docs/mcp-registry.html _site/lucius-testops-mcp-server/mcp-registry.html
          cp docs/mcp_manifest.json _site/lucius-testops-mcp-server/mcp_manifest.json
          # Make /lucius-testops-mcp-server/ resolve directly.
          cp docs/mcp-registry.html _site/lucius-testops-mcp-server/index.html

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v4.0.0
        with:
          path: _site

  pages-deploy:
    needs: pages-build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4.0.5

  pypi-publish:
    needs: python-quality
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Set up Python Environment
        uses: ./.github/actions/python-setup

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.13.0
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
